name: client_grpc
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  protobuf: ^3.1.0
  camera: ^0.10.5+6
  video_player: ^2.8.1
  path_provider: ^2.1.1
  file_picker: ^6.1.1
  grpc: ^3.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true